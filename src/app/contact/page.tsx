import { PageShell } from "@/components/layout/PageShell";

export default function ContactPage() {
  return (
    <PageShell
      title="Contact Us"
      subtitle="Tell us about your brand. We'll get back within 24 hours."
    >
      <p className="mt-12 text-slate-500">
        Contact form with Resend integration — coming in the next task.
      </p>
    </PageShell>
  );
}
